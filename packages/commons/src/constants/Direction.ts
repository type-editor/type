export enum Direction {
    Backward = -1,
    Forward = 1,
    // eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
    Up = -1,
    // eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
    Down = 1
}
